from tkinter import *
from PIL import ImageTk, Image

# Create the main window
root = Tk()
root.title("Stencil Generator")

# Stencil Image Sidebar
stencil_image = ImageTk.PhotoImage(Image.open("IMAGE PLACEHOLDER ADDRESS"))
stencil = Label(root, image = stencil_image)
stencil.grid(column = 0, row = 1)

# Start the GUI event loop
root.mainloop()
